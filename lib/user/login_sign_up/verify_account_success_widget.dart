import 'auth_success_widget.dart';

class VerifyAccountSuccessWidget extends AuthSuccessWidget {
  const VerifyAccountSuccessWidget({super.key})
      : super(
            title: "Your account has now been successfully verified.",
            subtitle:
                "You start chatting with pool operators and access premium services from the pool details screen.");
}
